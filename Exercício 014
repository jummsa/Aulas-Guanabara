# Escerva um programa que converta uma temperatura digitada em °C para °F. 

c = float(input('Informe a temperatura em °C:'))
f = (c*9/5) + 32

print('A temperatura de {:.1f}°C corresponde a {:.1f}ºF.'.format(c,f))
