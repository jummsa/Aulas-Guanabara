# Crie um programa que leia um número Real qualquer pelo teclado e mostre na tela sua posição inteira

n = float(input('Digite um número:'))

print('O valor digitado foi {} e sua porção inteira é {}.'.format(n, int(n)))
